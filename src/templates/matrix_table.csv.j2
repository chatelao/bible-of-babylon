{{ headers|join(',') }}
{%- for row in matrix %}
"{{ row.label }}",{% for cell in row.cells -%}
"{{ cell.value|format_value|replace('"', '""') }}"{{ "," if not loop.last }}
{%- endfor %}
{%- endfor -%}
