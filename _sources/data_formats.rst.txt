Data Format Patterns
====================



String
------


:description: Representation of text data.


Parameters:

* syntax: String

* notes: String



.. list-table:: String Comparison
   :widths: auto
   :header-rows: 1

   * - Language
     - Syntax
     - Notes
   * - JsonString
     - .. code-block:: json

           "Hello"
     - JSON strings must be enclosed in double quotes.
   * - XmlString
     - .. code-block:: xml

           <tag>Hello</tag>
     - XML values are text-based.
   * - YamlString
     - .. code-block:: yaml

           Hello or 'Hello' or "Hello"
     - Supports plain, single-quoted, and double-quoted strings.
   * - TomlString
     - .. code-block:: toml

           "Hello"
     - TOML strings must be double-quoted.
   * - CsvString
     - .. code-block:: text

           Hello or "Hello"
     - CSV values are typically plain text; quoting is used for values containing delimiters or newlines.
   * - FixlengthString
     - .. code-block:: text

           Fixed width string (e.g., 'Hello     ')
     - Data is defined by fixed positions and lengths rather than delimiters.



Number
------


:description: Representation of numeric data (integers or floating-point).


Parameters:

* syntax: String

* notes: String



.. list-table:: Number Comparison
   :widths: auto
   :header-rows: 1

   * - Language
     - Syntax
     - Notes
   * - JsonNumber
     - .. code-block:: json

           42 or 3.14
     - Supports integers and floating-point numbers.
   * - XmlNumber
     - .. code-block:: xml

           <tag>42</tag>
     - Numeric values are represented as text.
   * - YamlNumber
     - .. code-block:: yaml

           42 or 3.14
     - Standard numeric representations.
   * - TomlNumber
     - .. code-block:: toml

           42 or 3.14
     - Strictly typed numeric values.
   * - CsvNumber
     - .. code-block:: text

           42 or 3.14
     - Numeric values are typically plain text.
   * - FixlengthNumber
     - .. code-block:: text

           Fixed width number (e.g., '00042')
     - Data is defined by fixed positions and lengths rather than delimiters.



Boolean
-------


:description: Representation of truth values (true or false).


Parameters:

* syntax: String

* notes: String



.. list-table:: Boolean Comparison
   :widths: auto
   :header-rows: 1

   * - Language
     - Syntax
     - Notes
   * - JsonBoolean
     - .. code-block:: json

           true / false
     - Standard boolean literals.
   * - XmlBoolean
     - .. code-block:: xml

           <tag>true</tag>
     - Boolean values are typically 'true' or 'false'.
   * - YamlBoolean
     - .. code-block:: yaml

           true / false
     - YAML 1.2 uses true/false; YAML 1.1 also supported yes/no, on/off.
   * - TomlBoolean
     - .. code-block:: toml

           true / false
     - Standard boolean literals.
   * - CsvBoolean
     - .. code-block:: text

           true / false
     - Represented as text literals.
   * - FixlengthBoolean
     - .. code-block:: text

           Fixed width indicator (e.g., 'Y' / 'N')
     - Data is defined by fixed positions and lengths rather than delimiters.



Date
----


:description: Representation of date and time information.


Parameters:

* syntax: String

* notes: String



.. list-table:: Date Comparison
   :widths: auto
   :header-rows: 1

   * - Language
     - Syntax
     - Notes
   * - JsonDate
     - .. code-block:: json

           "2026-05-24"
     - JSON does not have a native date type; ISO 8601 strings are commonly used.
   * - XmlDate
     - .. code-block:: xml

           <tag>2026-05-24</tag>
     - ISO 8601 is the standard format for dates in XML (XSD date type).
   * - YamlDate
     - .. code-block:: yaml

           2026-05-24
     - YAML has native support for ISO 8601 dates (timestamp type).
   * - TomlDate
     - .. code-block:: toml

           2026-05-24
     - TOML has native support for RFC 3339 dates and times.
   * - CsvDate
     - .. code-block:: text

           2026-05-24
     - Usually represented as ISO 8601 strings.
   * - FixlengthDate
     - .. code-block:: text

           Fixed width date (e.g., '20260524')
     - Data is defined by fixed positions and lengths rather than delimiters.



Collection
----------


:description: Ordered sequence of elements (Arrays, Lists, or Sequences).


Parameters:

* elements: String

* syntax: String

* notes: String



.. list-table:: Collection Comparison
   :widths: auto
   :header-rows: 1

   * - Language
     - Syntax
     - Notes
   * - JsonCollection
     - .. code-block:: json

           [1, "two", true]
     - Elements are comma-separated and enclosed in square brackets.
   * - XmlCollection
     - .. code-block:: xml

           <list>
             <item>1</item>
             <item>2</item>
           </list>
     - Collections are typically represented by repeating elements under a parent.
   * - YamlCollection
     - .. code-block:: yaml

           - 1
           - two
           - true
     - Uses a dash followed by a space for each element.
   * - TomlCollection
     - .. code-block:: toml

           [1, 2, 3]
     - Arrays can contain values of different types since TOML 1.0.0.
   * - CsvCollection
     - .. code-block:: text

           1,two,true
     - A single row represents a collection of fields.
   * - FixlengthCollection
     - .. code-block:: text

           Field1Field2Field3
     - Collections are typically represented by a sequence of fields with pre-defined lengths.



Mapping
-------


:description: Unordered collection of key-value pairs (Objects, Maps, or Dictionaries).


Parameters:

* entries: String

* syntax: String

* notes: String



.. list-table:: Mapping Comparison
   :widths: auto
   :header-rows: 1

   * - Language
     - Syntax
     - Notes
   * - JsonMapping
     - .. code-block:: json

           {"key": "value", "num": 42}
     - Keys must be double-quoted strings.
   * - XmlMapping
     - .. code-block:: xml

           <object>
             <key>value</key>
             <num>42</num>
           </object>
     - Mappings are represented as nested elements.
   * - YamlMapping
     - .. code-block:: yaml

           key: value
           num: 42
     - Uses a colon followed by a space to separate key and value.
   * - TomlMapping
     - .. code-block:: toml

           key = "value"
           num = 42
     - Top-level or grouped using [headers].
   * - CsvMapping
     - .. code-block:: text

           Name,Age
           Alice,30
     - Mappings are established by associating header names with column values.
   * - FixlengthMapping
     - .. code-block:: text

           Pos 1-10: Name, 11-15: Age
     - Mappings are established by a schema defining which positions correspond to which fields.



Metadata
--------


:description: Way to attach metadata or attributes to data elements.


Parameters:

* syntax: String

* notes: String



.. list-table:: Metadata Comparison
   :widths: auto
   :header-rows: 1

   * - Language
     - Syntax
     - Notes
   * - JsonMetadata
     - N/A
     - JSON does not support metadata or attributes on elements; often simulated using underscore-prefixed keys (e.g., "_metadata": { ... }).
   * - XmlMetadata
     - .. code-block:: xml

           <element attr="value">Content</element>
     - XML has native support for attributes on elements.
   * - YamlMetadata
     - .. code-block:: yaml

           !!str "value" or !custom { key: val }
     - YAML supports tags to specify types or attach metadata to nodes.
   * - TomlMetadata
     - N/A
     - TOML does not support per-element metadata, though it uses headers for grouping.
   * - CsvMetadata
     - N/A
     - Standard CSV (RFC 4180) does not support metadata or attributes.
   * - FixlengthMetadata
     - N/A
     - Fixed-length formats do not natively support metadata or attributes within the data records.



SingleLineComment
-----------------


:description: Way to add non-executable explanatory text to the data on a single line.


Parameters:

* syntax: String

* notes: String



.. list-table:: SingleLineComment Comparison
   :widths: auto
   :header-rows: 1

   * - Language
     - Syntax
     - Notes
   * - JsonSingleLineComment
     - N/A
     - JSON does not natively support comments.
   * - XmlSingleLineComment
     - .. code-block:: xml

           <!-- comment -->
     - XML uses <!-- --> for comments.
   * - YamlSingleLineComment
     - .. code-block:: yaml

           # comment
     - YAML uses # for single-line comments.
   * - TomlSingleLineComment
     - .. code-block:: toml

           # comment
     - TOML uses # for single-line comments.
   * - CsvSingleLineComment
     - N/A
     - Standard CSV does not natively support comments; some variations use #.
   * - FixlengthSingleLineComment
     - N/A
     - Fixed-length formats generally do not support comments.



MultiLineComment
----------------


:description: Way to add non-executable explanatory text to the data spanning multiple lines.


Parameters:

* syntax: String

* notes: String



.. list-table:: MultiLineComment Comparison
   :widths: auto
   :header-rows: 1

   * - Language
     - Syntax
     - Notes
   * - JsonMultiLineComment
     - N/A
     - JSON does not natively support comments.
   * - XmlMultiLineComment
     - .. code-block:: xml

           <!--
             line 1
             line 2
           -->
     - XML uses the same syntax for single and multi-line comments.
   * - YamlMultiLineComment
     - .. code-block:: yaml

           # line 1
           # line 2
     - YAML does not have a native block comment; it uses multiple single-line comments.
   * - TomlMultiLineComment
     - .. code-block:: toml

           # line 1
           # line 2
     - TOML only supports single-line comments starting with #.
   * - CsvMultiLineComment
     - N/A
     - Standard CSV does not support multi-line comments.
   * - FixlengthMultiLineComment
     - N/A
     - Fixed-length formats generally do not support comments.



SchemaLink
----------


:description: Linking a data file to a schema for validation (e.g., JSON Schema, XSD).


Parameters:

* syntax: String

* notes: String



.. list-table:: SchemaLink Comparison
   :widths: auto
   :header-rows: 1

   * - Language
     - Syntax
     - Notes
   * - JsonSchemaLink
     - .. code-block:: json

           "\$schema": "http://json-schema.org/draft-07/schema#"
     - JSON uses the \$schema keyword to point to a JSON Schema file.
   * - XmlSchemaLink
     - .. code-block:: xml

           <root xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                 xsi:schemaLocation="http://example.com schema.xsd">
     - XML uses the xsi:schemaLocation attribute to link to an XSD.
   * - YamlSchemaLink
     - .. code-block:: yaml

           # yaml-language-server: \$schema=<url_or_path>
     - YAML often relies on editor-specific comments (like VS Code's language server) for schema linking.
   * - TomlSchemaLink
     - N/A
     - TOML does not have a native or widely standardized way to link to a schema within the file.
   * - CsvSchemaLink
     - N/A
     - CSV does not have a native schema linking mechanism; often defined in a sidecar file (e.g., CSVW metadata).
   * - FixlengthSchemaLink
     - N/A
     - Schema definition is usually external (e.g., a Copybook or a COBOL file descriptor).



Header
------


:description: Standardized header or declaration at the beginning of a file or document.


Parameters:

* syntax: String

* notes: String



.. list-table:: Header Comparison
   :widths: auto
   :header-rows: 1

   * - Language
     - Syntax
     - Notes
   * - JsonHeader
     - N/A
     - JSON files do not have a standardized header; they start directly with the data (object or array).
   * - XmlHeader
     - .. code-block:: xml

           <?xml version="1.0" encoding="UTF-8"?>
     - XML declaration specifies the version and encoding of the document.
   * - YamlHeader
     - .. code-block:: yaml

           %YAML 1.2
           ---
     - The %YAML directive (optional) specifies the version, and the triple dash (---) marks the start of a document.
   * - TomlHeader
     - N/A
     - TOML does not have a native file-level header or declaration.
   * - CsvHeader
     - .. code-block:: text

           Name,Age,City
     - The first row of a CSV file often contains the column names (headers).
   * - FixlengthHeader
     - .. code-block:: text

           H2026052400001
     - Fixed-length formats often use a 'Header' record (e.g., starting with 'H') containing metadata like date or batch ID.